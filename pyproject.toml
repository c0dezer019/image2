[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "image2"
version = "1.5.2"
description = "Convert images to colored ASCII or traditional ANSI art."
authors = [{name = "c0dezer019", email = "briandb1222@gmail.com"}]
license = {text = "MIT"}
requires-python = ">=3.14"
dependencies = ["Pillow", "cairosvg", "pyperclip"]

[project.scripts]
img2 = "image2:main"
[tool.setuptools]
py-modules = ["image2", "img2ansi", "img2ascii", "imgcommon", "imgsvg", "img2ui", "img2log", "img2webhook", "img2feedback", "img2bug"]
packages = ["_img2ui_data"]

[tool.setuptools.package-data]
"_img2ui_data" = ["docker-compose.yml"]

[tool.black]
line-length = 79
target-version = ['py314']
include = '\.pyi?$'
extend-exclude = '''
/(
  # directories
  \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | venv
  | _build
  | buck-out
  | build
  | dist
)/
'''
